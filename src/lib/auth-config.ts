// NextAuth has been removed. Using custom JWT authentication instead.
// This file is kept for backward compatibility but is no longer used.

export const authOptions = {
  // Empty config - NextAuth is disabled
}

