-- NEUTRALIZED MIGRATION (intentionally a no-op).
--
-- This migration was originally auto-generated by `prisma migrate dev`. It
-- DROPped the raw-SQL patent-search indexes (GIN / pg_trgm) and updatedAt
-- DB defaults that are created by earlier migrations but are not modeled in
-- schema.prisma. Those constructs are intentional, so dropping them is wrong.
--
-- It was also unapplyable on a fresh database because it referenced
-- "local_patents"."corpusSources" (created later by 20260619120000) before
-- that column exists.
--
-- The body has been replaced with a no-op so the migration history stays
-- linear across environments. The indexes/defaults are kept by their original
-- migrations, and schema.prisma now models them so this is not regenerated.
SELECT 1;
